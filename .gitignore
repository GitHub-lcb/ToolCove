# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust 构建产物：src-tauri 与 crates/* 各自的 target/ 都可重建，不入库
src-tauri/target/
crates/*/target/
# 标签排版引擎的 wasm 产物：**不入库**——它是编译结果，入库会出现「源码改了但 wasm 还是旧的」
# 这种最难查的问题。由 `npm run build:label-wasm` 生成到 mobile/public/，
# CI 的工作流在构建/测试前会先跑它（见 .github/workflows/mobile-release.yml 与 ci.yml）。
mobile/public/*.wasm

# 本地打包产物与临时文件
release-out
tmp
tmp-deploy
tmp-debug
# Playwright（E2E）的产物：失败截图/trace 与 HTML 报告，都是本机排查用，不入库
test-results
playwright-report
blob-report
playwright/.cache
# 根目录下随手起的临时排查脚本 / 输出：它们**不该**进仓库，而 `git add -A` 会顺手扫进去
# （已经犯过三次）。只匹配 `/tmp-*` 前缀，不写 `tmp*`——那会把仓库里的 tmp/ 目录一起挡掉。
/tmp-*
*.tmp

# WorkBuddy 会话的工作区数据（本地备忘/日志，不入库）
.workbuddy/

# 本机专用的 cargo 构建目录重定向（含本机绝对路径，不入库）
.cargo/config.toml

# ── 手机端（mobile/，重建中：见 docs/mobile-app-plan.md）─────────────────
# 旧铁路大亨悬浮面板已整体删除（含其 dist 例外规则）；以下为**新安卓 App 模块**要挡的东西。
# 便携 Android 工具链：JDK / Gradle / SDK 合计约 1.5 GB，谁都下得动，不该进仓库
mobile/.toolchain/
mobile/android/.toolchain/
# Gradle 增量构建的本地缓存（.lock/.bin/.probe），每次构建都会变，纯本机状态
mobile/.gradle/
mobile/android/.gradle/
# 工具链位置是机器相关的（SDK 路径），本地生成
mobile/android/local.properties
# Gradle 构建输出与收集到的 APK
mobile/android/build/
mobile/android/app/build/
mobile/android/out/

# 自动更新签名私钥（红线：绝不入库）
src-tauri/updater.key
# 商业化时期的授权私钥（相关功能已移除，留作本地存档也绝不入库）
scripts/.license-secrets/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
