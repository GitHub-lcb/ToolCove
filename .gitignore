# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# 本地打包产物与临时文件
release-out
tmp
tmp-deploy
tmp-debug
# 根目录下随手起的临时排查脚本 / 输出：它们**不该**进仓库，而 `git add -A` 会顺手扫进去
# （已经犯过三次）。只匹配 `/tmp-*` 前缀，不写 `tmp*`——那会把仓库里的 tmp/ 目录一起挡掉。
/tmp-*
*.tmp

# WorkBuddy 会话的工作区数据（本地备忘/日志，不入库）
.workbuddy/

# 本机专用的 cargo 构建目录重定向（含本机绝对路径，不入库）
.cargo/config.toml

# ── 手机版（mobile/）──────────────────────────────────────────────────
# 便携 Android 工具链：JDK / Gradle / SDK 合计约 1.5 GB，谁都下得动，不该进仓库
mobile/android/.toolchain/
# Gradle 增量构建的本地缓存（.lock/.bin/.probe），每次构建都会变，纯本机状态
mobile/android/.gradle/
# 工具链位置是机器相关的（SDK 路径），本地生成
mobile/android/local.properties
# Gradle 构建输出与收集到的 APK
mobile/android/build/
mobile/android/app/build/
mobile/android/out/
# 上面那条通用的 `dist` 规则（第 11 行，无斜杠 → 匹配任意层级）会把手机版 HUD 的
# 产物目录一并挡掉。可那一份是**最终产物**：单文件、无外部依赖，用手机浏览器直接
# 打开就能当网页版用，80 KB 也值得随源码一起分发。所以在这里显式放开。
# 注意 assets/index.html 是它的副本（APK 打包要用），同样入库——
# 这样离线环境直接 build-apk 也能出包，不必先跑一遍 rail-hud 构建。
!mobile/rail-hud/dist/
!mobile/rail-hud/dist/index.html

# 自动更新签名私钥（红线：绝不入库）
src-tauri/updater.key
# 商业化时期的授权私钥（相关功能已移除，留作本地存档也绝不入库）
scripts/.license-secrets/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
