<?xml version="1.0" encoding="utf-8"?>
<!--
  全屏应用，不是悬浮窗：没有 SYSTEM_ALERT_WINDOW / FOREGROUND_SERVICE / POST_NOTIFICATIONS。
  INTERNET：本地 loopback 服务需要它（127.0.0.1 的 socket 同样受该权限管辖），AI 请求、云同步与取更新清单也走它。
  REQUEST_INSTALL_PACKAGES：只服务"App 内检查更新 → 拉起系统安装页"。
    不声明它就没有 content:// 安装流程，用户只能自己到浏览器下载再装。
    未授权时应用会跳系统设置页引导授权（见 UpdateNative），而不是静默失败。
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToolCove"
        android:usesCleartextTraffic="true">
        <!-- usesCleartextTraffic：本地服务是 http://127.0.0.1，明文是刻意的（不出设备） -->

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <!-- adjustResize：软键盘弹出时页面收缩，表单不会被键盘盖住 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 只读暴露缓存里的安装包给系统安装器（自己实现，不引 androidx FileProvider）。
             authority 必须与 UpdateNative.AUTHORITY 一致；exported=true 是安装器要能访问，
             但它只能读到 apk 子目录下的文件（ApkProvider 里做了路径约束）。 -->
        <provider
            android:name=".ApkProvider"
            android:authorities="com.githublcb.toolcove.apk"
            android:exported="true"
            android:grantUriPermissions="true" />
    </application>
</manifest>
